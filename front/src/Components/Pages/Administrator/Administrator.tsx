import React from 'react'

import './Administrator.scss'
import CustomAlert from '../../../SharedComponents/CustomAlert/CustomAlert'

interface AdministratorProps {}

interface AdministratorState {}

class Administrator extends React.Component<AdministratorProps, AdministratorState> {
  constructor(props: AdministratorProps) {
    super(props)
  }

  render() {
    return <React.Fragment>Administrator</React.Fragment>
  }
}

export default Administrator
